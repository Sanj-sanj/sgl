---
import PopupNavigation from "./react/PopupNavigation";

interface Props {
  orientation: "horizontal" | "vertical";
}

const { orientation } = Astro.props;
---

<nav class="w-full flex">
  <ul
    class:list={[
      ` flex text-center items-center`,
      `${orientation === "vertical" && "flex-col items-center w-full justify-evenly"}`,
    ]}
  >
    <li
      class:list={`sm:ml-2.5 text-center px-2 relative flex${orientation === "vertical" && "w-full py-4"}`}
    >
      <a href="/">Home</a>
    </li>

    <PopupNavigation
      client:load
      text={"About"}
      subTexts={[
        { text: "About", path: "#" },
        { text: "Our Services", path: "#" },
        { text: "Environmental Policy", path: "#" },
      ]}
      orientation={orientation}
    />

    <PopupNavigation
      client:load
      text={"Contact Us"}
      subTexts={[
        { text: "Contact", path: "#" },
        { text: "Careers", path: "#" },
      ]}
      orientation={orientation}
    />
  </ul>
</nav>
